{% extends 'base.html.twig' %}
{%  block title 'Edit Pin #' ~ helo.id %}
{% block body %}
    <h1>Edit pin #{{ helo.id }}</h1>
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
    <!--<form method="post" action="">
    <input type="hidden" name="_token" value=" csrf_token('toid') #}}">
    <div>
        <label for="title">Title
        </label>
        <input id="title" name="title" type="text" value="hello" >
    </div>

    <div>
        <label for="age">Age
        </label>
        <input id="age" name="age" type="number" value="hello">
    </div>
    <input type="submit" value="submit">
</form>-->
{#    {{ form(maform) }}#}
      {{ form_start(maform) }}
    {{ form_widget(maform) }}
    <input type="submit" value="Edit">
    {{ form_end(maform) }}
{% endblock body %}