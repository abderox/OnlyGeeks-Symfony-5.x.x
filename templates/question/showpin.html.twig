{% extends 'base.html.twig' %}
{% block body %}
    <h3><a href="{{ path('app_create') }}"> Home </a></h3>
    <h3><a href="{{ path('app_show') }}"> Go back </a></h3>



            <div>
                <p>
                    {{ helos.age }}

                <div>
                    <p>
                        {{ helos.title|upper }}
                        <strong>  {{ random(5,20) }}</strong> liked your post
                    </p>
                    
                </div>
                <a href="{{ path('app_edit',{id:helos.id}) }}">Edit</a>
                </p>

            </div>



{% endblock body %}