<div class="container mt-5 pg-c " style="font-size: large">
<h1>Hi!</h1>

<p class="text-center font-weight-bold">To reset your password, please visit the following link</p>

<a class="text-decoration-none"  href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p class="text-center font-weight-bold">This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cheers!</p>
</div>